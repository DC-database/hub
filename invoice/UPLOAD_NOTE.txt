IBA 11.1.4 — Invoice Management Note Index Light Mode

Upload these changed files only:
1) app.js
2) index.html
3) version.json
4) js/app-data-cache.js
5) js/app-batch-entry-ui.js

Purpose:
- Batch Entry and Summary Note note suggestions now use the small invoice_note_index folder + browser cache.
- Opening Batch Entry / Summary Note must no longer call ensureInvoiceDataFetched() just to collect notes.
- Batch Entry status search now uses invoice_tasks_by_user/All active index, then exact-reads only matching invoices.
- Batch Entry save no longer full-downloads invoice_entries; it loads CSV/base data and exact-reads missing selected invoices only.
- Summary Note update no longer re-downloads all invoice_entries; it exact-reads missing visible rows only.

Firebase rule required and already added by user:
"invoice_note_index": {
  ".read": true,
  ".write": true,
  ".indexOn": ["lastUsedAt", "count", "text"]
}

WorkDesk and Inventory files were not touched.
