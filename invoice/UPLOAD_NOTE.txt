IBA 11.1.2 Upload Note

Upload/replace these files from this patch:
- index.html
- app.js
- version.json
- js/app-data-cache.js
- js/app-workdesk-dashboard.js

Purpose:
- Fixes the 11.1.1 console error: RangeError Maximum call stack size exceeded.
- Keeps CSV refresh throttle so Dashboard does not repeatedly reload POVALUE2.csv.
- Keeps Dashboard duplicate cleanup so old WorkDesk New Entry and live Invoice Entry status do not both appear.

After upload, hard refresh Chrome once.
