IBA 11.0.8 FINAL UPLOAD NOTE

Purpose:
- Final merged safety/UI update after 11.0.7 testing.
- Keeps the Chrome-only + single active tab guard.
- Keeps Test Mode / Firebase Blocker with Enter Test Preview.
- Keeps desktop numbered analog clock with no square/card background.
- Improves unsupported-browser screen: instead of a confusing Recheck-only action, it now provides a Copy Link for Chrome button.

Important behavior:
- Edge/unsupported browsers are blocked even in local/Test Mode.
- A browser page cannot reliably launch Google Chrome automatically because browsers block that for security.
- The button copies the current link so the user can paste it in Google Chrome.
- Duplicate Chrome tabs are still blocked/paused; only one active tab runs Firebase sync.

Upload/replace these files:
- index.html
- app.js
- version.json
- js/app-tab-guard.js
- js/app-test-mode.js
- js/app-data-cache.js
- js/app-time-clock.js

Testing:
1. Open index.html in Edge. It must show Google Chrome Required and must not allow Test Preview.
2. Click Copy Link for Chrome. It should copy the current link / show instruction.
3. Open index.html in Chrome from local folder. It should show Test Mode and Enter Test Preview.
4. Open a second Chrome tab. It should show the one-active-tab guard.
5. Open live GitHub link in Chrome. It should run LIVE MODE / Firebase Active.

Rollback:
- If any issue appears, restore 11.0.6.
