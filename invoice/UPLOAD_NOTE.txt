IBA 11.3.0 — Invoice Entry Modal Deletion Context Reset

Upload only these files:
- app.js
- index.html
- version.json

After upload, hard refresh Chrome with Ctrl+F5.

This patch fixes the Invoice Entry modal folder/add-to-deletion-list icon so it resets per current PO/invoice and does not keep the checked/disabled state from the previously opened PO.
