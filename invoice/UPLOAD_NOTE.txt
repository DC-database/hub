IBA 11.2.9 — Invoice Entry Modal Deletion Button State Guard

Upload only these files:
- app.js
- index.html
- version.json

Purpose:
- Fixes Invoice Entry modal folder icon/add-to-deletion-list button showing as already checked/disabled before you click it.
- The modal button now resets per current invoice/PO and mirrors the outside row button when a matching outside row is available.
- The actual add-to-deletion-list save logic remains the existing system logic.

After upload:
1. Hard refresh Chrome (Ctrl + F5).
2. Open Invoice Entry.
3. Open an invoice modal whose outside folder button is available.
4. The modal folder icon should also be clickable.
5. After clicking it, only then it should become check/disabled.
