IBA SYSTEM PATCH 11.1.7 - SUMMARY NOTE ONE-TIME NOTE INDEX REFRESH

Upload/replace these files only:
- app.js
- index.html
- version.json
- js/app-data-cache.js

Purpose:
- Summary Note still opens light and uses invoice_note_index first.
- Old historical Summary Note searches such as DON 2026 may not exist in invoice_note_index yet.
- If no indexed invoices are found, Generate Summary can ask for one-time legacy refresh.
- The one-time refresh reads invoice_entries only after confirmation, builds invoice_note_index for the typed note/search, then future searches are light.
- Supports flexible note matching: exact text, contains text, and all typed tokens (example DON 2026).

Protected:
- WorkDesk not touched.
- Inventory not touched.
- Invoice Management Summary Note note-index/backfill only.

After upload:
1. Hard refresh Chrome.
2. Open Summary Note.
3. Type DON 2026 in Current Note and click Generate.
4. If prompted, click OK to run the one-time index refresh.
5. Future DON 2026 searches should use invoice_note_index/exact reads instead of scanning all invoice_entries.
