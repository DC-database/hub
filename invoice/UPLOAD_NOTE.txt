10.8.5 PATCH ONLY

Upload/replace only:
- index.html
- js/app-workdesk-dashboard.js
- UPLOAD_NOTE.txt

Change:
- WorkDesk Dashboard search can now search globally when no status card is selected.
- Admin can type PO number, vendor name, invoice number, site, attention, or status and find matching cached Dashboard tasks.
- Search uses already-loaded/browser-cached Dashboard data only; typing in search does not trigger extra Firebase reads.
- If the global search result belongs to only one queue, that queue behavior is preserved:
  - For Summary keeps Vendor cards.
  - Retention, IPC, For SRV, etc. keep Site cards.
- If the search matches multiple queues, results show as global search results.
- No Firebase rules change.
- No save/workflow logic change.
- No app.js change.

After upload: hard refresh with Ctrl + F5.
